<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlantPedia" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/cream</item>
        <item name="android:colorControlNormal">@color/dark_green</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.PlantPedia" parent="Base.Theme.PlantPedia">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="ThemeOverlay.SearchView" parent="">
        <item name="android:editTextColor">@color/dark_green</item>
        <item name="android:textColorHint">@color/translucent_green</item>

        <!-- Add items from ThemeOverlay.Material3.Search  -->
        <item name="android:actionMenuTextColor">@color/dark_green</item>
        <item name="android:colorControlNormal">@color/dark_green</item>
        <item name="actionOverflowButtonStyle">@style/Widget.Material3.Search.ActionButton.Overflow</item>
        <item name="toolbarNavigationButtonStyle">@style/Widget.Material3.Search.Toolbar.Button.Navigation</item>
    </style>

    <style name="SplashScreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="windowSplashScreenBackground">@color/white</item>
    </style>
</resources>